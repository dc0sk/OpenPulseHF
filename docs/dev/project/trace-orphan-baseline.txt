# Production source files unclaimed by any capability at the bright line.
# Grandfathered: the checker warns on these. A file NOT listed here that is also
# unclaimed is a NEW orphan and fails the build. Shrink this list over time.
#
# Pruned 2026-09-18 (#1371): 86 -> 12 entries. The curation gave 67 of these files an owner
# while leaving them allowlisted, so a later removal from a `code:` list would have silently
# re-orphaned them. `trace.py` now FAILS on a baseline entry that is claimed (STALE-BASELINE)
# or that no longer exists, so this list can no longer drift out of step with the map.
#
# Every remaining entry has ZERO mutants: eleven are lib.rs/error.rs/mod.rs with no `fn` at all,
# and plugins/mfsk16/src/robust_ack.rs is TEST code (lib.rs declares it `#[cfg(test)] mod`),
# so owning any of them would add nothing to mutation scope or the dormancy join.
crates/openpulse-ardop/src/error.rs
crates/openpulse-core/src/error.rs
crates/openpulse-core/src/lib.rs
crates/openpulse-dsp/src/lib.rs
crates/openpulse-filexfer/src/error.rs
crates/openpulse-kiss/src/error.rs
crates/openpulse-modem/src/lib.rs
crates/openpulse-radio/src/error.rs
pki-tooling/src/api/mod.rs
plugins/js8/src/lib.rs
plugins/mfsk16/src/robust_ack.rs
