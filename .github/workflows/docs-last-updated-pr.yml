name: Stamp Docs last_updated in PRs

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: read

jobs:
  stamp-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Manual-only notice
        run: echo "Automatic PR stamping disabled. Run scripts/stamp-doc-last-updated.sh locally when needed."
