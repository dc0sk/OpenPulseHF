[build]
# The crate to build; trunk must be run from this directory.
target = "index.html"

[serve]
address = "127.0.0.1"
port = 8080
# Proxy WebSocket connections to the daemon's WS control port.
# Uncomment if running daemon on the same host.
# [[proxy]]
# backend = "ws://127.0.0.1:9001/ws"
# rewrite = "/ws"
